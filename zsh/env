# Ruby config -- add the rbenv to PATH
#export PATH="$HOME/.rbenv/bin:$PATH"

# required by rbenv
#eval "$(rbenv init -)"

# alternatively, use
#if which rbenv > /dev/null; then eval "$(rbenv init --no-rehash - zsh)"; fi
