autoload -Uz promptinit && promptinit # use the included prompt functionality

# pimp my prompt
prompt adam1

# right-hand prompt with exit status error and time
RPS1=%(?..[ %B%F{yellow}%?%b%f ])

# EOF
